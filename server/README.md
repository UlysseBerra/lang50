# `lang50` API & database

## How to use
- for testing purposes
  - while in `server/` directory
  - `$ ./mvnw spring-boot:run`
- for full build
  - while in `server/` directory
  - `$ ./mvnw clean install`
  - `$ cd target/`
  - `java -jar lang50-api-1.0.jar`